<h1>The "<%= @test_passage.test.title %>" Test was Completed!</h1>

<h2>
  Success
  <% success = @test_passage.correct_questions*100/@test_passage.test.questions.map{|q| q.answers.correct}.count %>
  <big class="<%= success >= 85 ? "passed" : "failed"  %>">
    <%= success %>%
  </big>
  <br>
  <br>
  <%= success >= 85 ? "Test passed!" : "Test failed!" %>
</h2>

<br>
<%= link_to "All Tests", tests_path %> .
